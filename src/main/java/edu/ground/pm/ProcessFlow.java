package edu.ground.pm;

import edu.ground.datapreparation.Triad;

public class ProcessFlow {

    public ProcessFlow(Triad processData) {
        //TODO: Still work to do, manly because there where no tests and there where errors by operation.
        //new GraphvizModeler(processData);
    }
}
